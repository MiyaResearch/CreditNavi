# 東京メトロ 運賃表機能 仕様設計（v1・2026-08-08）

対象: `index.html`（クレカ乗車運賃ナビ）へ新機能として追加する「東京メトロ 運賃表」画面。
本書は実装前の設計書。SPEC.md への統合は実装完了後に行う。

---

## 1. 目的とスコープ

### 1.1 目的
東京メトロ各駅のコンコースに掲出されている「東京メトロ きっぷ運賃表」（路線図の各駅に運賃を重ねた掲示物）を、
**全144駅ぶん、アプリ内で任意の駅を「当駅」に切り替えて閲覧できる**ようにする。

### 1.2 スコープ（v1）
| 項目 | 内容 |
|---|---|
| 対象事業者 | 東京メトロのみ（`fare.json` の `operator:"tokyometro"`、144駅） |
| 表示形式 | 路線図風マップ（SVG）。実物の掲示物に準拠 |
| 運賃種別 | きっぷ運賃（大・主）＋ IC運賃（小・併記）。実物と同じ2段表記 |
| 対象外（v1） | 都営乗り入れ運賃、他社連絡運賃、他事業者の運賃表、こども運賃、定期運賃 |

### 1.3 非目標
- 実物の掲示物のピクセル単位の再現（駅名の欧文併記レイアウト、注記の全文転載など）
- 経路探索との統合（v1では「この駅間を検索」への遷移のみ）

---

## 2. 前提: 既存資産の確認結果

| 資産 | 状態 |
|---|---|
| `fare.json` → `operators[operator="tokyometro"]` | 駅144件・OD運賃10,296件（144×143/2の完全対称行列）。`pairFormat:["fromIdx","toIdx","cash","ic"]` |
| 駅の同定 | `stations[].slug`（例 `AkasakaMitsuke`）が正規キー。`odptIds[]` に路線修飾ODPT ID を全件保持 |
| `odpt_Station_metro.json` | 186件（路線×駅）。**全件に `geo:lat` / `geo:long` / `odpt:stationCode` あり** |
| `odpt_Railway_metro.json` | 10路線（丸ノ内線分岐線を含む）。`odpt:stationOrder` / `odpt:color` / `odpt:lineCode` あり |
| 路線ナンバリング画像 | `路線ナンバリング/*.png` に9路線ぶん既存（`RN_ICON_FILE_BY_CODE`） |
| fare.json ↔ ODPT の突合 | **144駅すべてが `odptIds` 経由でODPT駅にマッチ（欠落0）** |

→ **座標データは ODPT の緯度経度から機械生成できる**。ゼロから手打ちする必要はない。

---

## 3. データ設計

> **【2026-08-08 更新】v1 の自動生成レイアウト（緯度経度＋オクトリニア化）は「路線図として見づらい」ため破棄。
> 実物の掲示物写真から座標を抽出する方式に変更し、`map_tokyometro.json`（v2）として実装済み。
> 以下 3.1〜3.2 は v2 の内容に差し替え済み。**

### 3.1 新規ファイル `map_tokyometro.json`

路線図の**幾何情報のみ**を持つ新ファイル。運賃は一切持たない（運賃は `fare.json` から実行時に引く）。

```jsonc
{
  "format": "metro-map",
  "version": 2,
  "operator": "tokyometro",
  "generated": "2026-08-08",
  "note": "駅座標は東京メトロ公式「きっぷ運賃表」掲示物の写真を射影変換で正対化し、運賃バッジ位置を抽出して作成した実物準拠レイアウト。",
  "viewBox": [0, 0, 3422, 1365],
  "badgeSize": [76, 48],
  "labelFontSize": 24,
  "lines": [
    {
      "code": "G",                       // odpt:lineCode
      "railway": "odpt.Railway:TokyoMetro.Ginza",
      "name": "銀座線",
      "color": "#FF9500",                // odpt:color
      "path": ["Shibuya", "OmoteSando", "..."],           // fare.json の slug 順序
      "codes": { "Shibuya": "G01", "OmoteSando": "G02" }  // 駅ナンバー
    }
  ],
  "stations": [
    {
      "slug": "AkasakaMitsuke", "name": "赤坂見附",
      "x": 1248, "y": 891,                                 // 運賃バッジの中心＝駅位置
      "label": { "dx": 0, "dy": 47, "anchor": "middle" },  // 駅名ラベルのオフセット
      "lines": ["G", "M"], "codes": ["G05", "M13"],
      "interchange": true
    }
  ]
}
```

**設計判断**
- ノードの同一性キーは `fare.json` の `slug`。運賃引きが `slugToIdx` 一発で済む。
- **駅位置＝運賃バッジの中心**。実物の掲示物と同じく、路線の上に運賃バッジが直接乗る。
  別途の駅マーカー（丸印）は描かない。
- 座標系は viewBox 内の抽象座標（正対化した掲示物の実測px）。緯度経度は持たない。
- 乗換駅は角丸長方形（`rx=10`）、それ以外は楕円（`rx=24`）。実物準拠。

### 3.2 `map_tokyometro.json` の生成手順（実施済み・`運賃表マップ生成/` に一式）

実物の掲示物写真1枚から、コンピュータビジョンで座標を機械抽出した。

| # | スクリプト | 処理 |
|---|---|---|
| 1 | `warp2.py` | 写真から地図面の四隅を検出し**射影変換で正対化**（3480×1550px、撮影時の傾き・遠近を除去） |
| 2 | `detect6.py` | 照明ムラを除去（ガウシアン正規化）した上で、運賃バッジの**暗い輪郭リングの内側（穴）**を多段しきい値で検出。白率・数字率・彩度で検証し **121個**を自動抽出 |
| 3 | `anchors.py` | 拡大タイル画像を目視して確定させた「検出番号 → 駅slug」の対応表（**108駅**） |
| 4 | `assign.py` | アンカーから**TPS（薄板スプライン）**で地理座標→掲示物座標の非線形写像を学習し、残りをハンガリアン法で最適割当（再現誤差 平均1.0px） |
| 5 | `missing.py` | 検出できなかった**25駅**（大手町・銀座・北千住など、密集部や乗換駅の角丸バッジ）の座標を、グリッド重畳画像から目視で実測 |
| 6 | `build.py` | 座標を統合し、45°格子から±7°以内の辺だけを軽く吸着（本来カーブしている辺は触らない）。viewBox に正規化 |
| 7 | `labels.py` | 駅名ラベル位置を、バッジ・他ラベル・路線との衝突コスト最小化で自動配置（**重なり0件**） |
| 8 | `offset.py` | **並行区間の分離**。ほぼ同一直線上で重なるセグメントを検出し（13回廊）、中心線間32pxで左右に振り分ける。単独駅を持つ路線（例: 外苑前を持つ銀座線）は動かさず、相手の路線だけを退避させるため、バッジは必ず自分の路線の上に乗る。折れ曲がった先の回廊でも左右が入れ替わらないよう、参照路線の進行方向で符号を揃える |
| 9 | `render.py` | 検証用SVG/PNG出力 |

**分離される並行区間（自動検出）**
- 小竹向原〜池袋（有楽町線／副都心線、和光市まで含め7区間）
- 渋谷〜表参道〜青山一丁目（銀座線／半蔵門線）※半蔵門線は外苑前に触れない
- 国会議事堂前〜霞ケ関（丸ノ内線／千代田線）、霞ケ関〜日比谷（千代田線／日比谷線）
- 市ケ谷〜飯田橋（南北線／有楽町線）

**なぜこの方式か** — 緯度経度からの自動生成（v1）では、都心部が潰れて実物とは似ても似つかない図になった。
実物の掲示物は長年の設計の積み重ねで最適化されており、**それを直接デジタル化するのが最短かつ最も正確**。

**再実行が必要になる場面** — 新駅開業・路線改廃で掲示物が改訂されたとき。
新しい掲示物の写真を1枚撮って `warp2.py` から流し直せば、アンカー表の差分修正だけで更新できる。

### 3.3 既存ファイルへの追加なし
`fare.json` は無改変。運賃は既に全ペアぶん存在する。

---

## 4. 画面設計

### 4.1 エントリポイント（検索画面）

`index.html` L.506 付近、`<div class="footnote" id="app-note">` の**直前**に追加:

```
  [セクション見出し] 運賃表
  ┌─────────────────────────────┐
  │ 🚇  東京メトロ 運賃表          ›│   ← 既存 .cell / .card-banner スタイル流用
  │     各駅の掲示運賃表を見る       │
  └─────────────────────────────┘
```
- `onclick="openFareTable()"`
- 位置は「お気に入り区間」「ポイント計算に使うカード」の下、フットノートの上（＝画面最下部）

### 4.2 新画面 `#scr-fare-table`（push画面）

既存の push 画面（`.screen.pushed` + `pushScreen()` / `popScreen()`）と同一構造。

```
┌──────────────────────────────────────┐
│ ‹ 戻る      東京メトロ 運賃表            │  ← .nav（既存）
├──────────────────────────────────────┤
│ ┌──────────────────────────────────┐ │
│ │ 当駅  [ 銀座 ]                     ›│ │  ← 当駅セレクタ（タップで駅選択シート）
│ └──────────────────────────────────┘ │
│ [ きっぷ・IC ]  ← 表示切替（任意・4.5）   │
├──────────────────────────────────────┤
│                                      │
│        （SVG 路線図マップ）             │
│         ピンチズーム / パン可            │
│                                      │
├──────────────────────────────────────┤
│ 凡例・注記（折りたたみ）                  │
└──────────────────────────────────────┘
```

- 当駅セレクタは**画面上部に固定**（sticky）。マップをスクロールしても常に見える。
- `switchTab()` L.1932 の「push画面リセット配列」に `"scr-fare-table"` を追加すること。

### 4.3 SVG マップの描画仕様

> **【2026-08-08 更新・描画方式の変更】**
> 路線の線・同一駅リンク・バッジの枠・駅名を**1枚のベース画像 `運賃表ベース.svg`（58KB）に焼き込み**、
> HTML側では**運賃の数字と当駅の塗りだけ**を重ねる方式に変更した。
> - ベース画像と運賃レイヤーは同一の `viewBox` を持ち、同じ `transform` で拡大縮小するため**位置が絶対にずれない**
> - 駅名に `textLength` + `lengthAdjust="spacingAndGlyphs"` を指定してあるので、
>   端末のフォントが違っても文字幅が変わらない＝レイアウトが崩れない
> - DOM要素数が 144×4 → 144×3（当たり判定＋数字2つ）に減り、当駅切替は `textContent` の書き換えのみ
> - 生成は `運賃表マップ生成/base.py`。座標や色を変えたら再生成する
>
> **レイヤ構造（下から）**
1. `<g id="ft-lines">` 路線ポリライン（`stroke` = 路線色, `stroke-width` 6, `stroke-linejoin:round`, `linecap:round`）
2. `<g id="ft-nodes">` 駅マーカー
   - 単独駅: 半径4の白丸＋路線色の縁取り
   - 乗換駅: 角丸長方形（複数路線を跨ぐ表現）または半径6の白丸＋濃い縁取り
   - **当駅**: 赤（`var(--red)`）の二重丸＋「当駅」ラベル（実物準拠）
3. `<g id="ft-fares">` 運賃バッジ
4. `<g id="ft-labels">` 駅名ラベル

**運賃バッジ（1駅1個）**
```
   ╭───────╮
   │  210  │   ← きっぷ運賃 / font-size 13 / font-weight 700
   │  209  │   ← IC運賃 / font-size 9 / 色 var(--label2)
   ╰───────╯
```
- 背景は白の角丸矩形＋薄いグレー枠（実物は白丸に黒文字）
- 駅マーカーの上（`dy = -14`）に配置。ラベルは下側に配置して重なりを避ける
- **運賃帯による色分け**: 枠線色を運賃帯で変える（180=緑 / 210=青 / 260=橙 / 300=赤 / 330=紫）。
  実物にはない演出だが、「いくらで行けるか」の把握を助ける。設定で off にできると良い（v1では常時on）
- **当駅**: バッジの代わりに「当駅」の赤バッジ
- **運賃0円**（同一駅扱い、13ペア）: 「同一駅」と表示（3.4/7.1 参照）

**駅名ラベル**
- font-size 24、`map_tokyometro.json` の `label.dx/dy/anchor` に従う（衝突コスト最小化で事前計算済み）
- **白フチ（ハロー）を下に敷いてから本体を描く**。`paint-order` 非対応環境でも確実に読めるよう、
  同じ文字を2回描画する（1枚目 `fill:none; stroke:#F7F4EC; stroke-width:6`、2枚目 `fill:#1C1C1E`）
- ズーム率が低いときは**乗換駅・主要駅のみ**表示（`minZoom` を駅ごとに持たせる案もあるが、
  v1は「乗換駅 or 当駅から180円圏」を優先表示とし、ズーム 1.4 倍以上で全駅表示）

### 4.4 当駅の切替

- 当駅セレクタ、または**マップ上の駅を直接タップ** → その駅が当駅になる（全バッジ再計算）
- 駅選択は既存の `#station-sheet`（`openPicker()` / `renderStationList()` / `stationMatches()`）を再利用。
  `pickTarget` に新値 `"faretable"` を追加し、確定時に `setFareTableOrigin(slug)` を呼ぶ。
  ただし**東京メトロの144駅のみ**にフィルタする必要があるため、`renderStationList()` に
  「事業者フィルタ」を渡せるようにする（`pickerOperatorFilter` グローバル）。
- 当駅は `localStorage`（`crekaFareNavi.fareTableOrigin.v1`）に保存し、次回起動時に復元。

### 4.5 表示切替（きっぷ / IC）
v1は「きっぷ大・IC小の併記」を既定とし、切替UIは**置かない**（実物準拠、情報量も足りる）。
※将来 IC のみ／きっぷのみに絞るトグルを上部に足せる余地は残す。

### 4.6 駅タップ時のアクション
駅ノードをタップ → 当駅を切り替える（既定動作）。
**ロングタップ / バッジタップ** → 小さなポップオーバー:
```
  銀座 → 北千住
  きっぷ 260円 / IC 252円
  [ この区間を検索 ]      ← doSearch() へ遷移（クレカ運賃・ポイントまで見られる）
```
`[この区間を検索]` は `currentOD` を設定して既存 `doSearch()` を呼び、`popScreen("scr-fare-table")`。
→ **既存機能への自然な導線になるので、この動線は v1 に入れる価値が高い。**

### 4.6a 同一駅リンク（実物準拠）

実物の掲示物では、駅名が違っても同一駅として扱われる駅（銀座↔銀座一丁目 など）が
**黒フチ＋白帯のブリッジ**で結ばれている。これを再現する。

- 対象ペアは `fare.json` の運賃が **0円**になっている東京メトロ内のペアから自動抽出（`links.py`）。
  バッジ間距離170px以下の**12組**を採用し、`map_tokyometro.json` の `sameStationLinks` に格納:
  新富町↔築地／淡路町↔新御茶ノ水／二重橋前↔東京／人形町↔水天宮前／銀座一丁目↔銀座／
  国会議事堂前↔溜池山王／大手町↔東京／虎ノ門ヒルズ↔虎ノ門／御茶ノ水↔新御茶ノ水／
  日比谷↔有楽町／赤坂見附↔永田町／仲御徒町↔上野広小路
- 日比谷↔東京 も0円だが、実物でも線が引かれておらず距離も離れているため除外（自動で除外される）
- 描画は黒 stroke-width 20 → 白 stroke-width 13 の2度塗り。バッジより下のレイヤに置く

### 4.7 凡例・注記（画面下部、折りたたみ）
- 路線凡例（路線ナンバリング画像＋路線名）
- 実物準拠の注記:
  - 表示額は**おとな運賃**。こども運賃は本アプリでは扱わない
  - きっぷ運賃とIC運賃は異なる場合がある
  - **綾瀬〜北千住は160円（IC 155円）**（JR常磐緩行線経由の特定運賃）
  - **目黒〜中目黒はきっぷ260円 / IC 227円**（他社線区間を含む特例）
  - 運賃には鉄道駅バリアフリー料金（おとな10円）が含まれる
  - 出典 `issued: 2023-03-18` / ODPT

---

## 5. 運賃取得ロジック

```js
// 初期化時に1度だけ構築
FT.slugToIdx  = {};                     // "Ginza" -> 42
FT.fareMatrix = new Int16Array(144*144*2);  // [cash, ic] を密行列に展開（約82KB、対称展開込み）
```
- `fare.json` の `fares[]`（10,296件）を **Int16Array の密行列に一度だけ展開**する。
  当駅を切り替えるたびに配列を線形探索すると 144回×10,296件になるため、初回展開が必須。
- 当駅切替時は `origin = slugToIdx[slug]` として `fareMatrix[(origin*144 + j)*2]` を143駅ぶん読むだけ。
- 自駅（origin === j）は「当駅」表示。
- **0円ペア**（赤坂見附↔永田町 など13組）は「同一駅」表示（グレー）。
  これは `fare.json` 上、乗換駅として同一駅扱いされているペア（`stationGroups` の `fareContinuous`）。

---

## 6. 操作・インタラクション

| 操作 | 挙動 |
|---|---|
| ピンチ / ダブルタップ | ズーム（0.8〜4.0倍）。SVG の `transform: scale()` で実装（`viewBox` 書換より軽い） |
| ドラッグ | パン |
| 駅タップ | 当駅を切替（バッジのテキストのみ更新） |
| バッジ長押し | ポップオーバー（4.6） |
| 「当駅へ戻る」FAB | 当駅が画面中心に来るようパン＋ズームリセット |
| 戻るボタン | `popScreen("scr-fare-table")` |

**初期表示**: 当駅が画面中央に来るようにパン。ズームは全体が収まる倍率。

---

## 7. データ上の注意（実装時に必ず反映）

1. **0円ペア13組** — 表示は「同一駅」。運賃0円と誤読させない。
2. **綾瀬↔北千住 160円/155円** — 唯一の160円。運賃帯の色分けテーブルに 160 を追加すること。
3. **目黒↔中目黒 IC 227円** — きっぷ260円と乖離。IC欄が他と揃わないが**正しい**ので丸めない。
4. **丸ノ内線分岐線（中野坂上〜方南町）** — `odpt.Railway:TokyoMetro.MarunouchiBranch` として
   本線と別レコード。マップ上は丸ノ内線色で分岐として描く。`lines` は10件になる。
5. **駅数の差** — ODPT 186件は「路線×駅」、fare.json 144件は「駅」。名寄せ後の 144 が正。
6. **運賃額の分布**（きっぷ）: 160×1 / 180×2,661 / 210×3,564 / 260×3,144 / 300×808 / 330×105 / 0×13。
   実物の掲示物と同じ 180/210/260/300/330 の5段階＋特例。

---

## 8. 既存コードへの変更点（index.html）

| 箇所 | 変更 |
|---|---|
| L.506 付近（検索画面 `#app-note` の直前） | 「東京メトロ 運賃表」ボタンを追加 |
| L.607 付近（push画面群の末尾） | `<div class="screen pushed" id="scr-fare-table">` を追加 |
| `switchTab()` L.1932 | リセット対象配列に `"scr-fare-table"` を追加 |
| `openPicker()` / `renderStationList()` | `pickTarget==="faretable"` 分岐＋事業者フィルタ対応 |
| `LS_KEYS` | `fareTableOrigin:"crekaFareNavi.fareTableOrigin.v1"` を追加 |
| 新規セクション `/* §ft 運賃表 */` | 描画・状態管理一式（`ftEnsureLoaded()` は `rnEnsureLoaded()` と同じ遅延fetch方式） |
| `sw.js` | `APP_SHELL` に `"./map_tokyometro.json"` と `"./運賃表ベース.svg"` を追加し、`CACHE_NAME` を `v14` に上げる |
| `SPEC.md` | 実装完了後に「§22 東京メトロ運賃表」として追記 |

**遅延ロード方針**: `map_tokyometro.json`（31KB）は初回の運賃表オープン時に fetch する
（既存の `rnEnsureLoaded()` と同じパターン）。初期起動を重くしない。

---

## 9. パフォーマンス方針

- SVG ノード総数の見積り: 路線10本 + 駅144 + バッジ144(各2 text) + ラベル144 ≒ **約600要素**。
  モバイル Safari で十分実用範囲。
- 当駅切替時は **DOM を再構築せず**、事前に生成済みの `<text>` の `textContent` と
  バッジ枠の `stroke` のみを更新する（144駅ぶんのループ、1ms 未満）。
- ズーム/パンは親 `<g>` の `transform` 更新のみ。

---

## 10. 実装フェーズ

| Phase | 内容 | 状態 |
|---|---|---|
| 1 | 掲示物写真の正対化＋運賃バッジ自動検出 | **完了** |
| 2 | 駅割当（アンカー＋TPS＋ハンガリアン法）／未検出25駅の実測 | **完了** |
| 3 | `map_tokyometro.json` 生成・ラベル自動配置 | **完了**（144駅・重なり0） |
| 4 | 単体プレビュー `運賃表プレビュー.html` | **完了** |
| 5 | index.html への組み込み（画面・当駅切替・検索導線） | 未着手 |
| 6 | sw.js / SPEC.md 更新、実機確認 | 未着手 |

## 11. 検証項目

- [ ] 144駅すべてで当駅にでき、バッジが143個表示される
- [ ] 当駅→各駅の運賃が `fare.json` の値と一致（全144×143ペアをスクリプトで突合）
- [ ] 綾瀬/北千住・目黒/中目黒・0円13組が仕様通りに表示される
- [ ] 路線ポリラインが全駅を通り、途切れ・逆順がない
- [ ] 駅名ラベルとバッジが重ならない（エディタの衝突ハイライトで0件）
- [ ] オフライン（機内モード）で運賃表が開ける
- [ ] iPhone 実機でピンチズーム・パンが破綻しない

---

## 12. 未決事項

1. **運賃帯の色分け**を入れるか（実物にはない）。→ 初期実装では入れ、実機で見て判断。
2. **駅ナンバー（G09 等）をマップに出すか**。出すと情報過多になる恐れ。→ v1では出さず、
   駅タップ時のポップオーバーにのみ表示。
3. **将来の他事業者展開**。ファイル名を `map_<operator>.json` 形式にしてあるので、
   都営・福岡市営への横展開時にファイルを増やすだけで済む。→ **命名は `map_tokyometro.json` を推奨**。
